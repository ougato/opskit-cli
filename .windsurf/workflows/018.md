1. 修复 `python3 main.py` 开发态启动时 venv 重进路径解析到系统 Python 导致重复 exec 卡住的问题
2. 更新 将 `click >= 8.1` 明确加入运行依赖，避免 Typer 版本变化后缺少 click
3. 更新 补充 `test_exec_reenter_uses_venv_launcher_path` 回归测试
4. 更新 验证 `python3 main.py --version` 与 `python3 main.py --help` 可快速返回
5. 更新 全量测试通过：`396 passed, 11 skipped`
