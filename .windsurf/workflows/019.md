1. 修复 x-ui 子菜单图标 variation selector 导致菜单项出现双空格
2. 修复 REALITY 密钥生成未覆盖 3x-ui 实际 xray-linux-amd64 路径的问题
3. 修改 x-ui 安装依赖与安装脚本执行方式，静默捕获 stdout / stderr，避免进度条被 apt / curl / tar 输出打乱
4. 更新 x-ui 安装依赖命令使用 DEBIAN_FRONTEND=noninteractive
5. 更新 自动化测试覆盖 x-ui REALITY 密钥生成与菜单 label 空格
6. 修复 安装流程未写入 3x-ui 面板账号、端口和 base path 导致后续 API 登录失败的问题
7. 修复 面板 API base path 末尾斜杠导致 URL 拼接不稳定的问题
8. 修复 3x-ui v3 登录接口需要 CSRF token 与 session cookie，导致面板 API 返回 403 的问题
9. 更新 面板 API 调用增加短重试，避免服务重启后端口尚未就绪导致连接拒绝
10. 修 3x-ui 登录 JSON success 判断，避免错误 cookie 导致 API 401
11. 修 x-ui client_traffics 默认禁用导致 Xray clients 为 null
12. 更新 Trojan 自签 TLS 证书与 allowInsecure 链接支持
13. 更新 x-ui 状态 local_port 诊断逻辑，区分公网隧道端口与本地监听端口
14. 更新测试覆盖客户端启用、流量表同步、Trojan 证书与链接参数
15. 修 验收流程使用 xray 客户端真实代理访问 Google 204
16. 更新 Tailscale 运维工具 Recipe，接入软件管理 devops 分类
17. 新增 Tailscale 安装、授权、诊断、管理与卸载自动化流程
18. 修 x-ui 卸载残留 `/usr/local/x-ui`、`/etc/x-ui`、service 与命令链接的问题
19. 更新 Tailscale 主题图标、中文 / 英文 i18n、打包 hidden import 与测试
20. 修改 x-ui 安装恢复使用当前服务器公网 IP 生成分享链接
21. 删除 x-ui 服务端 Recipe 的 Tailscale 依赖
22. 删除 x-ui 安装、诊断、状态字段中的 Tailscale 检测逻辑
23. 保留 Tailscale 为独立运维工具，不参与 x-ui 链接生成
24. 更新 Tailscale 安装流程加入 Exit Node 自动配置
25. 新增 Tailscale Exit Node NAT、MSS 与 IPv6 保护持久化服务
26. 更新 Tailscale 卸载流程清理 Exit Node 服务、脚本、sysctl 与防火墙规则
27. 补充 Tailscale Exit Node 安装与卸载测试
28. 记录 WSL 中 Nginx 安装失败的根因：`systemctl` 存在但 systemd 未运行
29. 计划新增通用服务管理兼容层，支持 systemd 与 SysV service 自动降级
30. 新增 core/service.py 服务兼容层，修复 WSL 中 systemctl 不可用导致的安装失败
31. Nginx 与 Docker 服务启停改用通用兼容层
32. 补充 service / Nginx WSL 降级测试
33. 修复 Docker 安装 / 卸载进度显示 i18n key 的问题
34. Docker 改为系统包安装模式，WSL / Ubuntu 下安装 `docker.io`
35. Docker 检测改为优先检查 apt 包状态，避免卸载后外部 CLI 造成误判
36. Docker 卸载包列表补全 docker.io / docker-ce / containerd / runc 相关包
37. 补充 Docker 安装、检测、本地化进度与卸载误判测试
38. 记录 Java JDK / Node.js 进度条显示裸 `check / download / remove` 的本地化问题
39. 计划对齐 GoLang 的 MultiStepProgress 本地化写法
40. Java JDK 安装 / 卸载进度对齐 GoLang 本地化文案
41. Node.js 安装 / 卸载进度对齐 GoLang 本地化文案
42. 扫描 Recipe 裸进度文案并补充 Java JDK / Node.js 本地化测试
43. 新增 RustDesk Server 运维工具设计：安装 hbbs / hbbr，输出 ID Server / Relay Server / Key
44. 计划修复运维工具超过 9 个时的列表分页问题
45. 计划调整 Tailscale / X-UI 图标并统一 X-UI 显示名
46. 实现 RustDesk Server Recipe，支持安装、卸载、诊断和客户端连接信息输出
47. 运维工具列表增加分页，避免超过 9 项后无法选择
48. 调整 Tailscale / X-UI / RustDesk 图标并统一 X-UI 显示名
49. 完成 RustDesk 安装、卸载、重装和本地端口验收
