1. 修复 x-ui 子菜单图标 variation selector 导致菜单项出现双空格
2. 修复 REALITY 密钥生成未覆盖 3x-ui 实际 xray-linux-amd64 路径的问题
3. 修改 x-ui 安装依赖与安装脚本执行方式，静默捕获 stdout / stderr，避免进度条被 apt / curl / tar 输出打乱
4. 更新 x-ui 安装依赖命令使用 DEBIAN_FRONTEND=noninteractive
5. 更新 自动化测试覆盖 x-ui REALITY 密钥生成与菜单 label 空格
6. 修复 安装流程未写入 3x-ui 面板账号、端口和 base path 导致后续 API 登录失败的问题
7. 修复 面板 API base path 末尾斜杠导致 URL 拼接不稳定的问题
8. 修复 3x-ui v3 登录接口需要 CSRF token 与 session cookie，导致面板 API 返回 403 的问题
9. 更新 面板 API 调用增加短重试，避免服务重启后端口尚未就绪导致连接拒绝
10. 修 3x-ui 登录 JSON success 判断，避免错误 cookie 导致 API 401
11. 修 x-ui client_traffics 默认禁用导致 Xray clients 为 null
12. 更新 Trojan 自签 TLS 证书与 allowInsecure 链接支持
13. 更新 x-ui 状态 local_port 诊断逻辑，区分公网隧道端口与本地监听端口
14. 更新测试覆盖客户端启用、流量表同步、Trojan 证书与链接参数
15. 修 验收流程使用 xray 客户端真实代理访问 Google 204
