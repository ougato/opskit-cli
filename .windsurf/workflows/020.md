1. 更新 plans/001.md 新增「外部插件平台（第三方扩展）」架构章节（插件目录 / plugin.yaml 清单 / python 与 exec 两种形态 / SDK / 加载流程 / 插件管理模块）
2. 新增 core/sdk.py 插件 SDK 稳定 API 层，SDK_API_VERSION = 1，re-export i18n / theme / prompt / runner / paths / logger / ModuleInfo
3. 新增 core/plugin.py 外部插件发现与加载：plugin.yaml 清单校验、python 进程内加载、exec 子进程执行、api_version 兼容检查、单插件故障隔离
4. 新增 core/paths.py plugins_dir()，支持 OPSKIT_PLUGINS_DIR 环境变量覆盖
5. 新增 core/constants.py DIR_PLUGINS / FILE_PLUGIN_MANIFEST / PLUGIN_API_VERSION 常量
6. 修改 core/module.py ModuleInfo 新增可选 label / icon 字段（外部插件菜单显示覆盖）
7. 修改 core/loader.py discover_modules() 追加外部插件发现，_SKIP_NAMES 增加 plugins
8. 修改 main.py 主菜单渲染支持 ModuleInfo.label / icon 覆盖
9. 新增 plugin/ 插件管理模块（列表 / 安装 git clone / 更新 git pull / 启用禁用 / 卸载）
10. 更新 core/locale/zh.yaml 与 en.yaml 新增 menu.plugin / module.plugin.desc / plugin.* 文案
11. 更新 core/themes/catppuccin.yaml 新增 modules.plugin 配色与 plugin 图标
12. 修改 build.py _SKIP_PKG 增加 plugins，避免开发插件目录被打进单文件
13. 新增 docs/plugin-spec.md 插件开发规范（清单字段 / 两种形态模板 / SDK API 清单 / 发布约定）
14. 新增 tests/test_plugin.py 覆盖插件发现、清单校验、api_version 不兼容、故障隔离、启停开关等 11 个用例
