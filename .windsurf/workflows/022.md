# 022 - ohmyzsh 非 root 切换 Shell 超时修复 + 插件开发文档完善

1. 修复 software/recipes/ohmyzsh/impl.py `_switch_default_shell()` 非 root 下 chsh 经 PAM 向 /dev/tty 要密码导致挂起 60s 超时：`_run()` 新增 `detach_tty` 参数（start_new_session 脱离控制终端，PAM 立即失败不挂起）
2. 修复 `_switch_default_shell()` 提权回退：仅 root 或免密 sudo 可用时才调用 run_as_root，避免 sudo 密码提示在捕获输出下挂到超时
3. 修改 core/privilege.py `_sudo_passwordless` / `_prime_sudo` 公开为 `sudo_passwordless` / `prime_sudo` 供 recipe 复用
4. 修改 software/recipes/ohmyzsh/impl.py 新增 `_prime_privilege_if_needed()`：非 root 且无免密 sudo 时在进度开始前交互式预热 sudo（提示一次密码），安装与「切换 Shell」入口均调用，切换失败不再视为安装失败（面板提示手动切换）
5. 修改 software/recipes/ohmyzsh/constants.py 新增 SUDO_COMMAND 常量
6. 更新 core/i18n.py 新增 `register_locale()`：插件注册自有文案（{lang: 嵌套 dict}），语言切换后仍生效，不得覆盖主程序已有 key
7. 更新 core/sdk.py 导出 `register_locale`（新增导出不递增 SDK_API_VERSION）
8. 更新 docs/plugin-spec.md 重写为完整开发者指南：10 分钟上手、目录、清单字段、python/exec 模板、entry 包名规范、i18n/第三方依赖约束、SDK 表、信任模型、开发调试与常见问题速查表、红线约束、发布约定与自查清单
9. 更新 .windsurf/plans/001.md SDK 层章节同步 register_locale
10. 更新 tests/test_i18n.py 新增 register_locale 注册/语言切换/不可覆盖内置 key 测试（含语言还原 fixture）
11. 更新 tests/test_ohmyzsh_shell.py 新增 11 个 Shell 切换/sudo 预热单测：已是 zsh、无 zsh、chsh 成功脱离 tty、非 root 无免密 sudo 快速失败且不调 run_as_root、root/免密 sudo 回退、chsh 超时、run_as_root 异常、预热跳过与仅提示一次
12. 全量测试 524 passed 11 skipped
