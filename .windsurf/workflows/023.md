# 023 - 热更新下载多源回退根治（坏镜像封禁页导致 v5 永远升不到 v6）

1. 修复 core/updater.py `_download_update()` 下载候选源构建：GitHub 直连（asset_url）始终作为兜底候选加入并去重保序，镜像排名坍缩为单一坏源（如 gh.con.sh 被封禁后对 HEAD 探针秒回、对真实下载吐 48 字节封禁页）时不再 5 次全打同一坏源、从不回退
2. 修复 core/updater.py 下载循环：切换到不同候选源时丢弃 .tmp 残包，禁止跨源 Range 续传把不同来源字节拼接成必然校验失败的文件
3. 删除 core/mirrors/sources.yaml github_releases cn 源中已被封禁的 gh.con.sh（探针可达但真实下载返回封禁页，会污染 mirror_cache 排名）
4. 更新 tests/test_auto_update_acceptance.py 新增 TestPoisonedMirrorFallback：R01 唯一排名源为封禁页坏代理时回退 GitHub 直连并 SHA256 校验成功、R02 get_sources 只剩挂掉代理时直连仍是兜底候选、R03 跨源切换丢弃残包不发 Range
5. 更新 .windsurf/plans/001.md 自动更新机制「国内代理可靠性说明」：补充直连兜底、坏源错误页由 SHA256 拒绝后自动换源、跨源残包丢弃、gh.con.sh 移除说明
