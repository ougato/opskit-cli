1. 新增 core/plugin_trust.py 插件信任存储：目录内容 sha256 指纹 + plugin_trust.yaml 读写 + is_trusted / grant / revoke / trusted_record
2. 新增 core/constants.py FILE_PLUGIN_TRUST 常量与默认配置 plugin.trusted_sources 可信源白名单
3. 修改 core/plugin.py 增加防崩守卫：import / register 期捕获 SystemExit，菜单入口 _guard_entry 拦截 BaseException 只报错回菜单，entry 包名防遮蔽校验（不得与 core / rich 等已有模块重名），sys.path 改尾部 append，exec 子进程异常全量捕获
4. 修改 core/plugin.py discover_plugins() 增加信任检查：未信任或指纹变化的插件不加载
5. 修改 core/plugin.py PluginManifest 新增 permissions 权限声明字段
6. 修改 plugin/commands.py 新增 trust_status / grant_trust / is_trusted_source / rollback_install，install 返回清单交由菜单层确认信任，remove 同步移除信任记录
7. 修改 plugin/menu.py 新增「信任插件」动作，列表增加信任状态列，安装流程增加第三方来源警告 + 概要展示（名称/版本/形态/权限）+ 信任确认（拒绝回滚删除），更新后指纹变化重新确认
8. 更新 core/locale/zh.yaml 与 en.yaml 新增 plugin.trust* / entry_failed / source_warning / col_perms 等文案
9. 更新 plans/001.md 插件平台章节：防崩守卫 / 信任机制 / 来源警告与权限声明 / plugin_trust.py 目录结构
10. 更新 docs/plugin-spec.md 新增信任模型与安全边界章节、permissions 清单字段说明
11. 更新 tests/test_plugin.py 增加信任 fixture 与 6 个安全用例（import 期 sys.exit 隔离 / 入口 SystemExit 守卫 / entry 遮蔽拒绝 / 未信任不加载 / 内容变化重确认 / 信任撤销），全量 511 passed
