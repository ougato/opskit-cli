1. 修复 WG 客户端安装时 DNS 被 wg-quick 覆盖导致内网域名失效
2. 修复 manage_client() 未安装时 key 4（添加隧道）被全量拦截无法执行的 bug
3. 修改 wireguard/client.py 新增 _detect_local_dns() 和 _merge_dns() 函数
4. 修改 wireguard/client.py manage_client() 将统一 not_installed 拦截改为 key 1/2/3 分别判断
5. 修改 _install_client_token() 写 WG 配置前自动合并本机 DNS 与 VPN DNS
6. 修改 update_client_token() 同步添加 DNS 合并逻辑并补充 detect_default_iface 导入
