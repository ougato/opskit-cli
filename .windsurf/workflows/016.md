1. 修复 wireguard/client.py _merge_dns() DNS 顺序错误（本机 DNS 优先改为 VPN DNS 优先）
2. 修改 wireguard/client.py 新增 _has_local_dns_service() 自动检测本机回环 DNS 服务
3. 修改 wireguard/client.py _install_client_token() 检测到本机 DNS 服务则不注入 VPN DNS
4. 修改 wireguard/client.py update_client_token() 同步 _has_local_dns_service() 判断逻辑
