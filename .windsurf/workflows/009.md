1. 修改 nginx/recipe.py 提取 _do_install(on_progress) 纯逻辑方法，原 install() 复用
2. 修改 nginx/recipe.py install() 改为调 _do_install(on_progress=sp.set_step_pct) 统一进度回调
3. 修改 wireguard/server.py _setup_nginx() 改为调 NginxRecipe()._do_install(on_progress=sp.set_step_pct)，透传 sp 参数
4. 删除 wireguard/server.py _install_nginx() 重复实现函数
5. 修改 wireguard/server.py step_keys 动态构建，nginx_mode==append 时不含安装 Nginx 步骤
6. 修改 wireguard/server.py 安装 Nginx 步骤仅在 nginx_mode==install 时调用 sp.step()
7. 更新 zh.yaml wireguard.step.install_nginx 文案从"配置 Nginx..."改为"安装 Nginx..."
