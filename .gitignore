# ── 构建自动生成 ───────────────────────────────────────────────────────────────
_registry.py

# ── 外部插件目录（开发模式本地安装，不入仓） ──────────────────────────────────
plugins/

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.pyo
*.pyd
*.so

# ── 虚拟环境 ──────────────────────────────────────────────────────────────────
.venv/
venv/
env/
.env/
ENV/

# ── 构建产物 ──────────────────────────────────────────────────────────────────
dist/
build/
_build/
*.spec
*.egg-info/
*.egg
MANIFEST

# ── Nuitka 产物 ───────────────────────────────────────────────────────────────
*.build/
*.dist/
*.onefile-build/
opskit.bin
opskit.exe

# ── 测试 / 覆盖率 ─────────────────────────────────────────────────────────────
.pytest_cache/
.coverage
htmlcov/
coverage.xml
*.coveragerc

# ── 用户数据 / 配置（运行时生成，不入库）──────────────────────────────────────
config/common.yaml
data/
logs/

# ── 临时 / 探针文件 ───────────────────────────────────────────────────────────
tests/_probe_*.py
tests/_child_*.py
tests/_ctrlc_result.txt
tests/**/token.txt

# ── Windsurf ──────────────────────────────────────────────────────────────────

# ── IDE / 编辑器 ──────────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# ── Windows 系统 ──────────────────────────────────────────────────────────────
desktop.ini
ehthumbs.db

# 运行时临时文件（自更新下载的二进制等）
cache/opskit.pending
cache/*.pending
